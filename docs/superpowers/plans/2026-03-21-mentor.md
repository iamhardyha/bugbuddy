# 멘토 인증 시스템 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 멘토 신청 → 관리자 승인/거절 워크플로우 + 알림 연동 구현

**Architecture:** MentorController(유저) + AdminMentorController(관리자) → MentorService → Repository. 승인/거절 시 Spring Event로 알림 발행. 기존 엔티티/Enum 활용, unique constraint 수정.

**Tech Stack:** Spring Boot 3.4.13, Java 21, Spring Data JPA, Lombok, MySQL

**Spec:** `docs/superpowers/specs/2026-03-21-mentor-design.md`

---

## 파일 구조

```
신규 생성:
  mentor/MentorController.java
  mentor/AdminMentorController.java
  mentor/MentorService.java
  mentor/MentorApplicationRepository.java
  mentor/MentorApplicationLinkRepository.java
  mentor/dto/MentorApplyRequest.java
  mentor/dto/MentorLinkRequest.java
  mentor/dto/MentorRejectRequest.java
  mentor/dto/MentorApplicationResponse.java
  mentor/dto/MentorLinkResponse.java
  mentor/dto/AdminMentorApplicationResponse.java
  notification/event/MentorApprovedEvent.java
  notification/event/MentorRejectedEvent.java

수정:
  model/entity/MentorApplication.java         — @SQLRestriction 추가, unique constraint 제거
  model/enums/NotificationType.java           — MENTOR_APPROVED, MENTOR_REJECTED
  global/response/ErrorCode.java              — 4개 추가, 1개 제거
  notification/NotificationService.java       — USER refType linkUrl/title 처리
  notification/dto/NotificationResponse.java  — (이미 linkUrl 있음, USER 처리 추가)
```

## 태스크 실행 순서

```
Task 1 (엔티티 수정 + Enum + ErrorCode)
  → Task 2 (Repository 2개)
  → Task 3 (DTO 6개)
  → Task 4 (알림 이벤트 + NotificationService USER 처리)
  → Task 5 (MentorService)
  → Task 6 (MentorController 유저용)
  → Task 7 (AdminMentorController 관리자용)
  → Task 8 (빌드 검증)
```

---

### Task 1: 엔티티 수정 + Enum + ErrorCode

**Files:**
- Modify: `backend/src/main/java/me/iamhardyha/bugbuddy/model/entity/MentorApplication.java`
- Modify: `backend/src/main/java/me/iamhardyha/bugbuddy/model/enums/NotificationType.java`
- Modify: `backend/src/main/java/me/iamhardyha/bugbuddy/global/response/ErrorCode.java`

- [ ] **Step 1: MentorApplication 엔티티 수정**

2가지 변경:

1-a. `@SQLRestriction("deleted_at IS NULL")` 추가 — `@Entity` 아래에:
```java
@Entity
@SQLRestriction("deleted_at IS NULL")
@Table(...)
```
import 추가: `import org.hibernate.annotations.SQLRestriction;`

1-b. `@Table`의 `uniqueConstraints` 배열에서 `uk_mentor_applications_user` 제거. 대신 `user_id` 인덱스만 유지.

**주의: `ddl-auto: update`는 기존 unique constraint를 자동으로 삭제하지 않음.** DB에 직접 실행 필요:
```sql
ALTER TABLE mentor_applications DROP INDEX uk_mentor_applications_user;
```
또는 서버 기동 전 `application.yml`에서 한 번만 실행할 수 있도록 안내.

- [ ] **Step 2: NotificationType에 추가**

```java
MENTOR_APPROVED,   // 멘토 인증 승인
MENTOR_REJECTED    // 멘토 신청 거절
```

- [ ] **Step 3: ErrorCode 수정**

추가:
```java
// 멘토 신청
MENTOR_ALREADY_PENDING(HttpStatus.CONFLICT, "이미 심사 중인 멘토 신청이 있습니다."),
MENTOR_ALREADY_APPROVED(HttpStatus.CONFLICT, "이미 인증 멘토입니다."),
MENTOR_REAPPLY_TOO_EARLY(HttpStatus.BAD_REQUEST, "재신청 대기 기간입니다. 7일 후 다시 시도해주세요."),
MENTOR_APPLICATION_NOT_PENDING(HttpStatus.BAD_REQUEST, "심사 대기 중인 신청이 아닙니다."),
```

제거: `MENTOR_APPLICATION_ALREADY_EXISTS` (MENTOR_ALREADY_PENDING으로 대체)

- [ ] **Step 4: 빌드 확인**

Run: `cd backend && ./gradlew compileJava`

---

### Task 2: Repository 2개

**Files:**
- Create: `backend/src/main/java/me/iamhardyha/bugbuddy/mentor/MentorApplicationRepository.java`
- Create: `backend/src/main/java/me/iamhardyha/bugbuddy/mentor/MentorApplicationLinkRepository.java`

- [ ] **Step 1: MentorApplicationRepository**

```java
package me.iamhardyha.bugbuddy.mentor;

import me.iamhardyha.bugbuddy.model.entity.MentorApplication;
import me.iamhardyha.bugbuddy.model.enums.MentorApplicationStatus;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;

import java.util.Optional;

public interface MentorApplicationRepository extends JpaRepository<MentorApplication, Long> {

    Optional<MentorApplication> findByUserId(Long userId);

    Page<MentorApplication> findAllByOrderByCreatedAtDesc(Pageable pageable);

    Page<MentorApplication> findAllByStatusOrderByCreatedAtDesc(
            MentorApplicationStatus status, Pageable pageable);

    /** 가장 최근 REJECTED 신청 조회 (soft-delete 포함 — 재신청 시 삭제한 것도 체크) */
    @Query(value = """
            SELECT * FROM mentor_applications
            WHERE user_id = :userId AND status = 'REJECTED' AND reviewed_at IS NOT NULL
            ORDER BY reviewed_at DESC LIMIT 1
            """, nativeQuery = true)
    Optional<MentorApplication> findLatestRejected(@Param("userId") Long userId);
}
```

`findLatestRejected`는 native query 사용 — `@SQLRestriction` 우회하여 soft-delete된 REJECTED 신청도 조회해야 재신청 대기 기간을 정확히 체크.

- [ ] **Step 2: MentorApplicationLinkRepository**

```java
package me.iamhardyha.bugbuddy.mentor;

import me.iamhardyha.bugbuddy.model.entity.MentorApplicationLink;
import org.springframework.data.jpa.repository.JpaRepository;

import java.util.List;

public interface MentorApplicationLinkRepository extends JpaRepository<MentorApplicationLink, Long> {

    List<MentorApplicationLink> findAllByMentorApplicationId(Long mentorApplicationId);
}
```

- [ ] **Step 3: 빌드 확인**

---

### Task 3: DTO 6개

**Files:**
- Create: `backend/src/main/java/me/iamhardyha/bugbuddy/mentor/dto/MentorApplyRequest.java`
- Create: `backend/src/main/java/me/iamhardyha/bugbuddy/mentor/dto/MentorLinkRequest.java`
- Create: `backend/src/main/java/me/iamhardyha/bugbuddy/mentor/dto/MentorRejectRequest.java`
- Create: `backend/src/main/java/me/iamhardyha/bugbuddy/mentor/dto/MentorApplicationResponse.java`
- Create: `backend/src/main/java/me/iamhardyha/bugbuddy/mentor/dto/MentorLinkResponse.java`
- Create: `backend/src/main/java/me/iamhardyha/bugbuddy/mentor/dto/AdminMentorApplicationResponse.java`

- [ ] **Step 1: 요청 DTO 3개**

```java
// MentorApplyRequest.java
public record MentorApplyRequest(
    @NotEmpty List<MentorLinkRequest> links,
    @NotBlank @Size(max = 5000) String q1Answer,
    @NotBlank @Size(max = 5000) String q2Answer
) {}

// MentorLinkRequest.java
public record MentorLinkRequest(
    @NotNull MentorApplicationLinkType linkType,
    @NotBlank @Size(max = 500) String url
) {}

// MentorRejectRequest.java
public record MentorRejectRequest(
    @NotBlank @Size(max = 500) String rejectionReason
) {}
```

- [ ] **Step 2: 응답 DTO 3개**

```java
// MentorLinkResponse.java
public record MentorLinkResponse(
    MentorApplicationLinkType linkType,
    String url
) {
    public static MentorLinkResponse from(MentorApplicationLink link) {
        return new MentorLinkResponse(link.getLinkType(), link.getUrl());
    }
}

// MentorApplicationResponse.java
public record MentorApplicationResponse(
    Long id,
    MentorApplicationStatus status,
    String q1Answer,
    String q2Answer,
    List<MentorLinkResponse> links,
    String rejectionReason,
    Instant createdAt,
    Instant reviewedAt
) {
    public static MentorApplicationResponse of(
            MentorApplication app, List<MentorApplicationLink> links) {
        return new MentorApplicationResponse(
            app.getId(), app.getStatus(),
            app.getQ1Answer(), app.getQ2Answer(),
            links.stream().map(MentorLinkResponse::from).toList(),
            app.getRejectionReason(),
            app.getCreatedAt(), app.getReviewedAt()
        );
    }
}

// AdminMentorApplicationResponse.java
public record AdminMentorApplicationResponse(
    Long id,
    MentorApplicationStatus status,
    String q1Answer,
    String q2Answer,
    List<MentorLinkResponse> links,
    String rejectionReason,
    Instant createdAt,
    Instant reviewedAt,
    Long applicantUserId,
    String applicantNickname,
    String applicantEmail
) {
    public static AdminMentorApplicationResponse of(
            MentorApplication app, List<MentorApplicationLink> links, UserEntity user) {
        return new AdminMentorApplicationResponse(
            app.getId(), app.getStatus(),
            app.getQ1Answer(), app.getQ2Answer(),
            links.stream().map(MentorLinkResponse::from).toList(),
            app.getRejectionReason(),
            app.getCreatedAt(), app.getReviewedAt(),
            user.getId(), user.getNickname(), user.getEmail()
        );
    }
}
```

- [ ] **Step 3: 빌드 확인**

---

### Task 4: 알림 이벤트 + NotificationService USER 처리

**Files:**
- Create: `backend/src/main/java/me/iamhardyha/bugbuddy/notification/event/MentorApprovedEvent.java`
- Create: `backend/src/main/java/me/iamhardyha/bugbuddy/notification/event/MentorRejectedEvent.java`
- Modify: `backend/src/main/java/me/iamhardyha/bugbuddy/notification/NotificationService.java`

- [ ] **Step 1: 이벤트 record 2개**

기존 NotificationEvent 인터페이스 구현:

```java
// MentorApprovedEvent.java
public record MentorApprovedEvent(
    Long triggerUserId,
    Long recipientUserId,
    Long refId
) implements NotificationEvent {
    @Override public NotificationType notificationType() { return NotificationType.MENTOR_APPROVED; }
    @Override public ReferenceType refType() { return ReferenceType.USER; }
}

// MentorRejectedEvent.java
public record MentorRejectedEvent(
    Long triggerUserId,
    Long recipientUserId,
    Long refId
) implements NotificationEvent {
    @Override public NotificationType notificationType() { return NotificationType.MENTOR_REJECTED; }
    @Override public ReferenceType refType() { return ReferenceType.USER; }
}
```

- [ ] **Step 2: NotificationService — USER refType 처리 추가**

`getNotifications()` 메서드의 title switch에 `case USER -> null;` 추가.
linkUrl switch에 `case USER -> "/settings/profile";` 추가.

현재 default가 title=null, linkUrl="/notifications"이므로, USER 케이스를 명시적으로 추가:

```java
// title switch (기존 default 앞에 추가)
case USER -> null;

// linkUrl switch (기존 default 앞에 추가)
case USER -> "/settings/profile";
```

- [ ] **Step 3: 빌드 확인**

---

### Task 5: MentorService

**Files:**
- Create: `backend/src/main/java/me/iamhardyha/bugbuddy/mentor/MentorService.java`

- [ ] **Step 1: 서비스 전체 생성**

주입 의존성: `MentorApplicationRepository`, `MentorApplicationLinkRepository`, `UserRepository`, `ApplicationEventPublisher`

클래스 레벨: `@Service`, `@RequiredArgsConstructor`, `@Transactional(readOnly = true)`

메서드:

**`apply(Long userId, MentorApplyRequest request)` — `@Transactional`**
1. UserEntity 조회 (USER_NOT_FOUND)
2. mentorStatus == PENDING → MENTOR_ALREADY_PENDING
3. mentorStatus == APPROVED → MENTOR_ALREADY_APPROVED
4. findLatestRejected 조회 → reviewedAt + 7일 체크 → MENTOR_REAPPLY_TOO_EARLY
5. 기존 활성 신청이 있으면 (REJECTED 상태) soft-delete
6. MentorApplication 생성 (setter 패턴, status=PENDING)
7. MentorApplicationLink 다건 저장 (saveAll)
8. user.setMentorStatus(PENDING)
9. MentorApplicationResponse 반환

**`getMyApplication(Long userId)`**
1. findByUserId 조회, 없으면 null 반환
2. links 배치 조회 → MentorApplicationResponse.of()

**`getApplications(MentorApplicationStatus status, Pageable pageable)`**
1. status가 null이면 전체, 아니면 상태 필터
2. Page<MentorApplication> 반환 → map to AdminMentorApplicationResponse
3. 각 application의 userId로 UserEntity 배치 조회 (N+1 방지)

**`getApplicationDetail(Long applicationId)`**
1. 신청 조회 (NOT_FOUND)
2. links + user 조회 → AdminMentorApplicationResponse.of()

**`approve(Long applicationId, Long adminUserId)` — `@Transactional`**
1. 신청 조회, status != PENDING → MENTOR_APPLICATION_NOT_PENDING
2. app.setStatus(APPROVED), setReviewerUserId, setReviewedAt
3. user.setMentorStatus(APPROVED)
4. eventPublisher.publishEvent(new MentorApprovedEvent(adminUserId, app.getUserId(), app.getId()))
   — refId는 applicationId 사용 (재신청 시 알림 중복 방지 — userId 사용 시 두 번째 승인/거절 알림이 차단됨)

**`reject(Long applicationId, Long adminUserId, MentorRejectRequest request)` — `@Transactional`**
1. 신청 조회, status != PENDING → MENTOR_APPLICATION_NOT_PENDING
2. app.setStatus(REJECTED), setReviewerUserId, setReviewedAt, setRejectionReason
3. user.setMentorStatus(NONE)
4. eventPublisher.publishEvent(new MentorRejectedEvent(adminUserId, app.getUserId(), app.getId()))
   — refId는 applicationId 사용 (동일 이유)

**getApplications N+1 방지:** application page에서 userIds 수집 → userRepository.findAllById() 배치 → Map<Long, UserEntity> 조합.

- [ ] **Step 2: 빌드 확인**

---

### Task 6: MentorController (유저용)

**Files:**
- Create: `backend/src/main/java/me/iamhardyha/bugbuddy/mentor/MentorController.java`

- [ ] **Step 1: 컨트롤러 생성**

```java
@RestController
@RequestMapping("/api/mentor")
@RequiredArgsConstructor
public class MentorController {

    private final MentorService mentorService;

    @PostMapping("/apply")
    public ResponseEntity<ApiResponse<MentorApplicationResponse>> apply(
            @AuthenticationPrincipal Long userId,
            @RequestBody @Valid MentorApplyRequest request) {
        MentorApplicationResponse response = mentorService.apply(userId, request);
        return ResponseEntity.status(HttpStatus.CREATED).body(ApiResponse.ok(response));
    }

    @GetMapping("/apply/me")
    public ResponseEntity<ApiResponse<MentorApplicationResponse>> getMyApplication(
            @AuthenticationPrincipal Long userId) {
        MentorApplicationResponse response = mentorService.getMyApplication(userId);
        return ResponseEntity.ok(ApiResponse.ok(response));
    }
}
```

- [ ] **Step 2: 빌드 확인**

---

### Task 7: AdminMentorController (관리자용)

**Files:**
- Create: `backend/src/main/java/me/iamhardyha/bugbuddy/mentor/AdminMentorController.java`

- [ ] **Step 1: 관리자 컨트롤러 생성**

```java
@RestController
@RequestMapping("/api/admin/mentor/applications")
@RequiredArgsConstructor
public class AdminMentorController {

    private final MentorService mentorService;

    @GetMapping
    public ResponseEntity<ApiResponse<Page<AdminMentorApplicationResponse>>> getApplications(
            @RequestParam(required = false) MentorApplicationStatus status,
            @PageableDefault(size = 20, sort = "createdAt", direction = Sort.Direction.DESC) Pageable pageable,
            @AuthenticationPrincipal Long adminUserId) {
        Page<AdminMentorApplicationResponse> response = mentorService.getApplications(status, pageable);
        return ResponseEntity.ok(ApiResponse.ok(response));
    }

    @GetMapping("/{id}")
    public ResponseEntity<ApiResponse<AdminMentorApplicationResponse>> getApplication(
            @PathVariable Long id,
            @AuthenticationPrincipal Long adminUserId) {
        AdminMentorApplicationResponse response = mentorService.getApplicationDetail(id);
        return ResponseEntity.ok(ApiResponse.ok(response));
    }

    @PatchMapping("/{id}/approve")
    public ResponseEntity<ApiResponse<Void>> approve(
            @PathVariable Long id,
            @AuthenticationPrincipal Long adminUserId) {
        mentorService.approve(id, adminUserId);
        return ResponseEntity.ok(ApiResponse.ok());
    }

    @PatchMapping("/{id}/reject")
    public ResponseEntity<ApiResponse<Void>> reject(
            @PathVariable Long id,
            @AuthenticationPrincipal Long adminUserId,
            @RequestBody @Valid MentorRejectRequest request) {
        mentorService.reject(id, adminUserId, request);
        return ResponseEntity.ok(ApiResponse.ok());
    }
}
```

SecurityConfig의 `.requestMatchers("/api/admin/**").hasRole("ADMIN")`가 이미 커버.

- [ ] **Step 2: 빌드 확인**

---

### Task 8: 전체 빌드 검증 + Obsidian 갱신

- [ ] **Step 1: 전체 빌드**

Run: `cd backend && ./gradlew clean build -x test`

- [ ] **Step 2: Obsidian 문서 갱신**

`Obsidian Vault/develop/logos/backend/멘토 인증 시스템 설계.md` 상태를 "구현 완료"로 갱신
